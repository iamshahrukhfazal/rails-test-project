<%= form_with(model: post, local: true) do |form| %>
 

    <div class="field">
    <%= form.label :content %>
    <%= form.rich_text_area :content, :class=>"form-control", :id=>"exampleFormControlTextarea1", :rows=>"3"%>
  </div>
  <div class="field">
    <%= form.label :status %>
    <input type="text" disabled name="post[status]" value="published"/>

  </div>
 <div class="actions">
    <%= form.submit :class=>"btn btn-primary w-100" %>
  </div> 
<% end %>
